/**
 * @pvotly/vue — declarative Vue 3 bindings for pvotly.
 *
 * Remember to import the stylesheet once in your app:
 * ```ts
 * import '@pvotly/web/styles.css';
 * ```
 */
export { PivotTable, default } from './PivotTable';

// Re-export engine + web types/values for convenience.
export * from '@pvotly/web';
